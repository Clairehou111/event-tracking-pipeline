spring:
  application:
    name: event-tracking-pipeline_xx

tracking:
  kafka:
    topic: EVENT_TRACKING_LOG_COLLECTOR
    enabled: true

logging:
  file:
    path: ${user.home}/logs
  level:
    com.example: INFO

#switch enable it
bigdataLog:
  active: true
